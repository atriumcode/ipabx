; ==================================================================
; CONFIGURAÇÃO REALTIME - PBX MODERNO ENTERPRISE
; Integração com PostgreSQL para dados dinâmicos
; ==================================================================

[settings]
; PJSIP Realtime
ps_endpoints => pgsql,asterisk,ps_endpoints
ps_auths => pgsql,asterisk,ps_auths
ps_aors => pgsql,asterisk,ps_aors
ps_domain_aliases => pgsql,asterisk,ps_domain_aliases
ps_endpoint_id_ips => pgsql,asterisk,ps_endpoint_id_ips

; SIP Realtime (chan_sip)
sippeers => pgsql,asterisk,sip_users
sipusers => pgsql,asterisk,sip_users

; Filas
queues => pgsql,asterisk,queues
queue_members => pgsql,asterisk,queue_members

; CDR
cdr => pgsql,asterisk,cdr

; Voicemail
voicemail => pgsql,asterisk,voicemail_users
